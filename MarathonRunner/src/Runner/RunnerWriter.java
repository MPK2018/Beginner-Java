package Runner;

/**
 * @author {Mugdha Phadke}
 *
 */
public interface RunnerWriter {
	
	    boolean addRunner(Runner p);
	  //  boolean updateRunner(Runner p);
	    boolean deleteRunner(Runner p);
	 
}
